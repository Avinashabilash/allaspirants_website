specVersion: "v1beta"
serviceId: "image"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "image-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
